29a30,33
> #ifndef MIN 
> 	#define MIN( x, y ) ( (x) < (y) ? (x) : (y) )
> #endif
> 
221c225
<           ? std::min<int>( (PacketSize - (int((size_t(ptr)/sizeof(Scalar))) & PacketAlignedMask))
---
>           ? MIN( (PacketSize - (int((size_t(ptr)/sizeof(Scalar))) & PacketAlignedMask))
