11,13c11,18
< IF(WIN32)
<   INCLUDE( ${WX_BUILD_SETTINGS_FILE} )
<   INCLUDE( ${WX_DEPEND_FILE} )
---
> IF(WIN32) 
>   IF(CMAKE_VERSION VERSION_GREATER 2.8)   
>      MESSAGE(STATUS "WX_BUILD_SETTINGS_FILE NOT USED")   
>   ELSE(CMAKE_VERSION VERSION_GREATER 2.8)      
>      INCLUDE( ${WX_BUILD_SETTINGS_FILE} )   
>   ENDIF(CMAKE_VERSION VERSION_GREATER 2.8) 
>  
>   INCLUDE( ${WX_DEPEND_FILE} ) 
